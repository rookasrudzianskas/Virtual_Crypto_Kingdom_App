import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    root: {
        alignItems: "center",
        // flex: 1,
    },
    image: {
        height: "30%",
        aspectRatio: 1,
    }
});

export default styles;
